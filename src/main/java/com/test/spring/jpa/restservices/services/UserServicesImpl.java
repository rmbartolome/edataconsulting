package com.test.spring.jpa.restservices.services;

import org.springframework.stereotype.Service;

@Service
public class UserServicesImpl {

    /*
    public String save(String user, String pass, User newUser){
        if(auth(user, pass)){
            save(newUser);
            return "success";
        }
        return "Error authentication user";
    }

    private boolean auth(String username, String pass){
        User userAuth = findByName(username);

        if(userAuth != null && userAuth.getPassword(),equals(pass)){
            return true;
        }
        return false;
    }*/
}
